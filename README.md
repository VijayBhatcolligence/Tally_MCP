# Tally_MCP

Deterministic accounting test datasets and Tally MCP automation artifacts.

The `dataset/` folder contains generated Foundry-Tally equivalence fixtures, expected reports, Tally import payloads, and verification summaries.

